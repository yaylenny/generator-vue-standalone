{
  "name": "<%= slug %>",
  "version": "1.0.0",
  "description": "<%= description %>",
  "main": "dist/<%= slug %>.js",
  "scripts": {
    "dev": "npm run dev-demo | npm run dev-library",
    "dev-demo": "webpack -d --watch --config demo.webpack.config.js",
    "dev-library": "webpack -d --watch --config webpack.config.js",
    "build": "npm run build-demo && npm run build-library",
    "build-demo": "webpack --config demo.webpack.config.js",
    "build-library": "webpack --config webpack.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "<%= author %>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-loader": "^7.1.1",
    "babel-preset-es2015": "^6.24.1",
    "css-loader": "^0.28.4",
    "html-loader": "^0.4.5",
    "node-sass": "^4.5.3",
    "sass-loader": "^6.0.6",
    "style-loader": "^0.18.2",
    "vue-loader": "^12.2.2",
    "vue-template-compiler": "^2.4.1",
    "webpack": "^3.3.0"
  },
  "dependencies": {
    "vue": "^2.4.2"<% if(addons.length){%>,<%}%> <% for( var i=0, len=addons.length; i<len; i++ ){ %>
    "<%= addons[i].value %>": "<%= addons[i].version %>"<% if((i+1)<len){%>,<%}%><%}%>
  },
  "vuelibs":{
    "demo":{
      "template": "demo/dist/demo.html",
      "script": "demo/dist/demo.js"
    }
  }
}
